#include "distance.h"
#include <iostream>

int main()
{
	/*Distance* array = new Distance[5];
	for (int i = 0; i < 5; i++)
	{
		array[i].Read();
	}

	Distance result;
	result.SetData(0, 0);

	for (int i = 0; i < 5; i++)
	{
		result.Add(array[i]);
	}

	result.Print();
	delete[] array;

	Distance d5(2, 5.5);
	d5.Print();*/

	Distance d6(2);
	d6.Print();

	return 0;
}
