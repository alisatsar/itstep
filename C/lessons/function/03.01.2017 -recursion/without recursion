#include <stdio.h>

void printStars(int starCount);

int main()
{
	int starCount = 10;
	printStars(starCount);
}

void printStars(int starCount)
{
	for (int i = 0; i < starCount; i++)
	{
		printf("*");
	}
	printf("\n");
}
