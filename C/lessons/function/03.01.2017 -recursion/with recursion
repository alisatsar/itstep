#include <stdio.h>

void printStars(int starCount);

int main()
{
	int starCount = 10;
	printStars(starCount);
}

void printStars(int starCount)
{
	if (starCount > 0)
	{
		printf("*");
		printStars(starCount - 1);
	}
}
